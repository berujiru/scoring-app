// Scores endpoints removed — use /api/judging (JudgingRow) instead.
// This file kept as an empty router to avoid accidental imports elsewhere.
import { Router } from 'express';
const router = Router();
export default router;
