// Scores controller removed. Endpoints have been deleted.
// If you need to restore score-related functionality, use the /api/judging
// endpoints which work with the JudgingRow model.

// Intentionally empty file to avoid accidental imports.
